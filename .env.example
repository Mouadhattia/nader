# Frontend Environment Variables
# Optional for local development. Copy to .env.local only if you need to override defaults.

# Backend API URL.
# Leave unset in production so the frontend uses the same deployed domain.
VITE_API_URL=http://localhost:5000
